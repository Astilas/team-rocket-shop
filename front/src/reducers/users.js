const initialState = {

};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case 'ok':
      return {

      };

    default:
      return state;
  }
};
export default reducer;
